# Veyrafish 升级方向与架构演进建议

> 基于 2026-04-21 对当前仓库的真实抽样分析，而不是仅基于 README 宣传描述。

## 一、先说结论

Veyrafish 现在最适合走的，不是“直接拆微服务”，而是：

1. 先把它从“文件协议驱动的多进程 Demo 型系统”升级成“模块化单体 + 明确任务边界 + 可观测”的生产前架构。
2. 再把 Agent 编排从 `Flask + Streamlit 子进程 + forum.log + markdown 文件` 过渡到“API 层 / Worker 层 / 状态层”三层结构。
3. 最后才考虑更强的 Agent runtime、前后端分离、插件化市场能力。

如果只选一条主路线，我建议：

**推荐主路线：模块化单体 2.0**

- Web/API：`FastAPI`
- 任务执行：任务队列 + Worker（容器化部署时可用 `Celery`，Windows 原生开发要避免把 Celery 当唯一运行方式）
- Agent 编排：先保留现有节点思想，第二阶段再引入 `LangGraph`
- 依赖治理：`pyproject.toml + uv`
- 可观测性：`OpenTelemetry`
- 前端：先 API-first，再决定是否拆 React/Vite

这条路线对当前项目最稳，因为它保留了已有的 Agent 业务逻辑和 ReportEngine 资产，同时解决最阻塞二开的问题：耦合、状态分散、协议脆弱、测试难跑。

## 二、项目现状判断

## 2.1 值得保留的部分

### 1. 领域拆分已经有雏形

`QueryEngine`、`MediaEngine`、`InsightEngine`、`ForumEngine`、`ReportEngine` 的职责边界是清晰的。虽然运行方式比较原始，但目录层面的模块分工是成立的。

### 2. ReportEngine 是当前最像“平台能力”的模块

`ReportEngine` 已经有模板选择、布局设计、字数规划、章节生成、IR 校验、HTML/PDF 渲染等完整链路。这部分不是简单脚本，已经接近可沉淀的核心引擎能力，后续应继续加强，而不是推倒。

### 3. 配置与模型适配已经朝统一方向走了

根目录 `config.py` 使用 `pydantic-settings`；多个引擎也使用 Settings 模型。这说明项目已经有“配置显式化”的意识，只是还没有完全收敛。

### 4. 多 Agent 产品形态已经能讲清楚

这个项目不是纯搜索器，也不是单报告工具，而是“搜集-讨论-汇总-生成报告”的完整链路。架构升级时要保住这个产品特征。

## 2.2 当前真正卡升级的地方

### 1. 主编排层仍然是进程脚本式架构

`app.py` 直接管理三个 Streamlit 子进程，并通过 `subprocess.Popen()` 拉起它们。这种方式适合快速 Demo，不适合持续二开，因为：

- 运行拓扑固定
- 生命周期管理脆弱
- 测试与部署边界混乱
- Web 层和任务执行层耦合过深

### 2. 模块通信仍然依赖文件协议

Forum 协作依赖 `forum.log`，ReportEngine 依赖 `*_streamlit_reports/*.md` 与 `logs/report_baseline.json` 判断输入是否就绪。这说明当前系统的真实“总线”不是内存事件、数据库状态或任务队列，而是磁盘文件。

这会直接带来：

- 并发一致性问题
- 状态追踪困难
- 可恢复性差
- 本地跑得通但线上难治理

### 3. 配置模型存在重复定义和漂移风险

根配置和各 Engine 内部都定义了 `BaseSettings` 模型，而且字段、默认值、约束并不完全一致。继续迭代下去，配置会越来越难维护。

### 4. 包边界还不够干净

多个模块存在 `sys.path` 拼接、目录回退导入、日志格式兼容分支等现象，说明当前模块化更多是“目录模块化”，还不是“可独立演进的包模块化”。

### 5. 测试保护层偏薄

现有测试主要集中在 `ForumEngine` 日志解析和 `ReportEngine` 局部 IR 清洗，说明团队已经开始补回归，但覆盖范围远不足以支撑大规模升级。并且本地环境中连 `pytest`、`loguru` 都未安装成功，测试入口无法直接执行。

### 6. 前端仍然是控制台式单页壳

`templates/index.html` 承担了配置、日志、控制面板、报告查看等多职责。它能用，但不利于后续做权限、历史任务、团队协作、模板管理和可视化追踪。

## 三、升级优先级

## P0：基础治理升级

这是最该先做的一层。没有这一层，后面引入再多新技术，都会变成新的复杂度。

### P0-1. 迁移到 `pyproject.toml + uv`

为什么先做：

- 当前仓库仍以 `requirements.txt` 为主，且环境一致性较弱
- 测试依赖和运行依赖没有形成稳定治理
- `uv` 已经支持标准 `pyproject.toml` 依赖和依赖分组，适合这个项目逐步收敛开发/测试/爬虫/模型依赖

建议做法：

- 保留 `requirements.txt` 作为过渡产物
- 新增 `pyproject.toml`
- 按 `core` / `web` / `crawler` / `ml` / `dev` 分组
- 把 `pytest`、`ruff`、`black`、类型检查工具放进开发组

### P0-2. 收敛配置体系

目标：

- 只保留一个权威配置入口
- 各 Engine 只定义自身扩展配置
- 所有路径、模型名、API Key、开关项都可追溯

建议做法：

- 根 `config.py` 升级为全局配置源
- 各 Engine 配置改为局部子模型或 namespaced settings
- 显式区分 `runtime config`、`engine config`、`deployment config`

### P0-3. 建立统一的数据契约

现在各模块之间大量靠字符串、Markdown、日志行和隐式字典传递状态。应该升级为：

- Agent 输入模型
- Agent 输出模型
- Forum 消息模型
- Report 输入模型
- Task 状态模型

建议用 `Pydantic v2` 统一表达，并在关键边界上校验。

### P0-4. 补齐测试分层

至少拆成四层：

- 单元测试：节点、工具、解析器、状态机
- 契约测试：Engine 输出、Forum 消息、Report IR
- 集成测试：一个查询从三引擎到报告装配
- 回归测试：固定样例输入 -> 固定报告结构检查

当前阶段不必追求全覆盖，但要先建立“升级后不会悄悄破协议”的底线。

### P0-5. 接入可观测性

建议先做最小版：

- 请求链路 trace
- 每个 Agent 任务的结构化日志
- 关键阶段 metrics
- 任务 ID、报告 ID、query ID 统一贯穿

这一步最适合引入 `OpenTelemetry`。对于这种多阶段、多模块、多供应商调用的系统，光看文本日志已经不够了。

## P1：架构升级

这一层才是“真正让项目变得能长期演进”的关键。

### P1-1. 用 `FastAPI` 替代当前主编排入口

不是因为 Flask 不能用，而是因为这个项目已经明显需要：

- 更清晰的 API 边界
- 更好的异步能力
- 生命周期管理
- 更自然的 WebSocket/SSE 支持
- 更标准的请求/响应模型

适合的迁移方式：

- 先保留现有路由语义
- 把 `app.py` 重构为 `api/`、`services/`、`workers/` 分层
- 用 `lifespan` 管理共享资源和启动/关闭逻辑
- 把当前对子进程的启动控制从请求线程里抽离出去

### P1-2. 把“文件总线”替换成“任务状态层”

当前最需要被替换的不是某个 LLM，而是通信方式。

建议目标：

- Agent 运行结果进任务存储
- Forum 消息进事件流或状态存储
- ReportEngine 从任务状态读取输入，而不是扫目录
- 前端只读任务状态和事件流

最保守实现：

- PostgreSQL + 任务表 + 事件表

更进一步：

- Redis 做运行时事件/缓存
- PostgreSQL 做结果持久化

### P1-3. 引入任务队列，不再由 Web 请求直接驱动长任务

当前分析任务、报告任务、爬虫任务都属于长任务，不应该与 Web 进程强耦合。

建议：

- Web/API 只负责创建任务和返回 task id
- Worker 负责执行 Query/Media/Insight/Report 流程
- 前端通过 SSE/WebSocket 订阅任务事件

选型建议：

- 容器化、Linux 部署优先：`Celery`
- 如果要强兼容本地 Windows 原生开发，队列层要提供本地退化模式，避免把 Celery 当成唯一运行时

### P1-4. Agent 编排从“隐式循环”升级为“显式状态图”

这是 Veyrafish 最值得引入现代 Agent runtime 的地方。

当前项目已经有：

- 状态对象
- 节点
- 反思循环
- 多 Agent 协作

这与 `LangGraph` 的状态图思路天然契合。适合的方式不是“全仓库立刻改写”，而是：

- 先把单个 Engine 的节点流程图谱化
- 再把 Forum 协作抽象为显式节点
- 最后把 Report 生成与多 Engine 汇总纳入统一状态图

这样升级的收益是：

- durable execution
- 明确状态边界
- 更好的人在环干预
- 更清晰的前端可视化

### P1-5. 拆出真正的共享核心包

建议新增一个类似 `veyrafish_core/` 的共享层，沉淀：

- LLM client 抽象
- 公共状态模型
- 通用消息模型
- 重试、限流、日志、trace 封装
- 文件与任务 ID 规则

否则现在这些能力会继续在各 Engine 中复制。

## P2：产品化增强

这层不一定马上做，但会决定项目是否能继续长成平台。

### P2-1. 前端走 API-first，再考虑 React/Vite

不建议第一步就推翻成前后端完全分离，因为后端协议目前还不稳定。

推荐顺序：

1. 先把 API 和任务状态层做好
2. 当前 HTML 壳继续用一段时间
3. 如果要做任务历史、模板管理、配置中心、协作视图，再切 React/Vite

### P2-2. 做模板与工具的插件化边界

这个项目未来最有价值的扩展点不是“多加几个模型”，而是：

- 新行业模板
- 新搜索/数据库工具
- 新 Agent
- 新报告块类型

所以应该尽早把插件边界设计出来，而不是继续用 `if/else + prompt + path` 扩。

### P2-3. 增加评估与回放能力

后续如果要持续迭代 Agent 质量，必须补：

- 固定任务集
- 结果评分
- 运行轨迹回放
- Prompt / tool / model 变更对比

否则每次升级都只能“感觉更好了”。

### P2-4. 引入知识与检索层

当前项目强在联网搜索和数据库分析，但对于：

- 历史上下文
- 内部知识沉淀
- 模板规范
- 长期会话记忆

还缺一个稳定承载层。后续可以考虑把报告模板、行业词典、平台规则、策略卡片纳入统一检索层，而不是散落在 prompt 和代码里。

## 四、推荐技术栈，不是越新越好，而是越合适越好

## 4.1 我建议引入的

### `FastAPI`

适合原因：

- 更适合 API-first 的任务型系统
- 生命周期、WebSocket、SSE、请求模型更顺手
- 和 Pydantic v2 体系贴合

### `uv`

适合原因：

- 能把当前依赖治理标准化到 `pyproject.toml`
- 便于区分开发、测试、ML、爬虫等依赖组
- 更适合作为后续 CI / 容器构建的统一入口

### `OpenTelemetry`

适合原因：

- 这个项目天然是多阶段、多模块、多外部依赖的链路
- trace + metric + log 统一后，才能真正定位性能和质量问题

### `LangGraph`

适合原因：

- 当前系统已经有“节点 + 状态 + 循环 + 协作”的结构基础
- 比起自己继续堆 if/for/log 文件，更适合进入可恢复、可视化、可干预的 Agent runtime

### `Celery` 或同类任务队列

适合原因：

- 分析任务和报告任务本质上是异步长任务
- 需要脱离 Web 请求线程
- 需要可重试、可监控、可并发调度

但注意：

- 如果开发环境长期以 Windows 原生为主，不能只依赖 Celery 的本地体验
- 更合理的做法是：容器内使用正式 Worker，本地保留同步/简化执行模式

## 4.2 我不建议现在就做的

### 1. 直接拆微服务

原因：

- 当前模块契约还没有稳定
- 文件协议都还没收敛，拆服务只会把隐式耦合变成分布式耦合
- 团队会先花大量时间在网关、部署、排障、数据一致性，而不是在产品能力上

### 2. 立刻重写前端

原因：

- 后端协议尚未稳定
- 现在重写 UI，后续 API 一变还得再返工

### 3. 大规模替换所有模型与 Prompt 框架

原因：

- 当前瓶颈更多在运行架构和工程化，不在“没有最潮框架”
- 先把运行边界、状态边界、评估边界做好，再谈模型层最优解

## 五、推荐主路线

### 阶段 A：工程治理

- 引入 `pyproject.toml + uv`
- 收敛配置体系
- 统一数据模型和任务模型
- 建 CI 和测试分层
- 加最小版 OpenTelemetry

### 阶段 B：运行架构升级

- Flask 主入口迁到 FastAPI
- Web 层与 Worker 层分离
- 任务不再由请求线程直接驱动
- 文件总线迁到任务状态层

### 阶段 C：Agent runtime 升级

- 单 Engine 图谱化
- Forum 协作状态化
- Report 汇总纳入统一状态图
- 增加回放、评估和人工介入能力

### 阶段 D：产品化

- API-first 前端
- 模板/工具插件机制
- 历史任务、评估面板、团队协作能力

## 六、30/60/90 天路线图

## 0-30 天

- 增加 `pyproject.toml`
- 把依赖拆组
- 整理统一配置入口
- 补测试基线
- 统一任务 ID / report ID / trace ID
- 给当前架构加结构化日志和最小追踪

目标：

- 让仓库先“可开发、可测试、可定位问题”

## 30-60 天

- 新建 FastAPI API 层
- 抽离任务服务层
- 引入队列或后台 worker
- ReportEngine 改为读任务状态，而不是扫目录
- Forum 协作从日志文件过渡到事件模型

目标：

- 让系统从 Demo 运行态进入稳定服务态

## 60-90 天

- 选择一个 Engine 做 LangGraph 化试点
- 增加任务回放和评估
- 设计插件化边界
- 决定是否拆前端

目标：

- 让多 Agent 逻辑真正进入可持续演进状态

## 七、如果只能选 5 个最值的升级点

1. `pyproject.toml + uv` 统一依赖治理
2. 统一配置模型与任务数据契约
3. `FastAPI + Worker` 替代 `Flask + Streamlit 子进程`
4. 文件协议迁移到任务状态层
5. 用 `OpenTelemetry` 建立可观测性，并在第二阶段引入 `LangGraph`

## 八、一句话判断

Veyrafish 现在的问题，不是“技术不够新”，而是“业务能力已经超过当前运行架构的承载能力”。下一阶段最正确的升级，不是追求更潮的 Agent 框架，而是先把它升级成一个真正能承载多 Agent 产品演进的工程系统。

## 九、外部技术参考

以下判断额外参考了官方资料：

- FastAPI 官方文档：后台任务、SSE、WebSocket、`lifespan` 生命周期管理
- uv 官方文档：`pyproject.toml` 依赖声明、开发依赖组、锁定与同步
- OpenTelemetry 官方文档：Python 自动埋点与 traces / metrics / logs
- LangGraph 官方文档：长运行、状态化、可恢复的 Agent 编排
- Celery 官方文档：任务队列、工作流编排与平台约束

这些资料的作用不是让项目“追新”，而是帮助确认本次推荐路线使用的能力在 2026-04-21 仍然是成熟且持续演进的。
